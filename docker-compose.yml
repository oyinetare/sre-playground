# version: "3.8"

services:
  app:
    container_name: sre-playground-app # to force specific name
    build: .
    ports:
      - "8000:8000"
    environment:
      - AWS_ENDPOINT_URL=http://localstack:4566
      - DATABASE_URL=postgresql://admin:password@postgres:5432/sredb
      - ENVIRONMENT=development
      - REDIS_URL=redis://redis:6379
    depends_on:
      - postgres
      - localstack
      - redis
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - sre-network

  # Localstack for AWS Services
  localstack:
    image: localstack/localstack:latest
    container_name: sre-playground-localstack
    ports:
      - "4566:4566"
    environment:
      - SERVICES=s3,sqs,dynamodb,secretsmanager,cloudwatch
      - DEBUG=1
      - EDGE_PORT=4566
    # volumes:
    #   - "./tmp/localstack:/tmp/localstack"
    #   - "/var/run/docker.sock:/var/run/docker.sock"
    networks:
      - sre-network

  postgres:
    image: postgres:15-alpine
    container_name: sre-playground-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: sredb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - sre-network

  # Prometheus for metrics collection
  prometheus:
    image: prom/prometheus:latest
    container_name: sre-playground-prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    networks:
      - sre-network

  # Grafana for visualization
  grafana:
    image: grafana/grafana:latest
    container_name: sre-playground-grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - sre-network

  redis:
    image: redis:7-alpine
    container_name: sre-playground-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - sre-network

volumes:
  postgres_data:
  prometheus_data:
  grafana_data:
  redis_data:

networks:
  sre-network:
    driver: bridge
